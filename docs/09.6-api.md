# Dashboard Economização

# 09.6 — API

Status: Ativo
Projeto: Dashboard Economização
Tipo: Padrão Oficial de Desenvolvimento

---

# 1. Objetivo

Este documento define o padrão oficial do contrato da API interna do Dashboard Economização.

A API interna é implementada em Node.js com TypeScript utilizando Fastify, executada como processo independente do frontend, conforme ADR-006 e ADR-032 do documento 08.

O objetivo deste padrão é garantir que a API:

- possua contrato previsível, consistente e estável entre módulos;
- seja a autoridade sobre regras de negócio, autorização e cálculos financeiros;
- imponha isolamento multiempresa em todas as operações;
- valide integralmente toda entrada recebida;
- retorne erros em formato uniforme e interpretável pelo frontend;
- nunca exponha segredos, tokens externos ou detalhes internos de infraestrutura;
- permaneça a única superfície de acesso a dados utilizada pelo frontend.

Este documento define o contrato.

A organização interna de camadas, serviços e repositórios pertence ao documento 09.3.

---

# 2. Escopo

Este documento se aplica a toda rota exposta pela API interna.

Estão dentro do escopo:

- estilo de contrato e uso de REST;
- versionamento;
- métodos HTTP;
- códigos de status;
- parâmetros de rota;
- query strings;
- filtros;
- ordenação;
- paginação;
- formato de resposta;
- formato de erro;
- validação;
- autenticação;
- contexto de tenant;
- idempotência;
- limites e proteção de superfície.

Está fora do escopo deste documento:

- estrutura interna de módulos e camadas do backend, definida no documento 09.3;
- mecanismos criptográficos e política de sessão, definidos no documento 09.7;
- formato de logs, definido no documento 09.11;
- integração com Conta Azul, definida no documento 04.

---

# 3. Regras Obrigatórias

## 3.1 A API é a autoridade

A API interna é a única autoridade sobre:

- autenticação e autorização;
- resolução do tenant efetivo;
- cálculos financeiros oficiais;
- normalização de dados sincronizados;
- validação de entrada;
- regras de negócio.

Nenhuma decisão crítica poderá ser delegada ao cliente.

## 3.2 tenantId do cliente nunca é confiável

Esta é a regra mais crítica deste documento.

O contexto de tenant deverá ser derivado da sessão autenticada no servidor.

Nas rotas do painel do cliente, o identificador de empresa não é aceito no caminho, na query nem no corpo da requisição. Caso ele apareça, deverá ser ignorado, e o tenant efetivo permanecerá o derivado da sessão, conforme o documento 09.9.

Nas rotas administrativas, o identificador de empresa pode aparecer no caminho, e nesse caso deverá ser tratado como solicitação, nunca como autorização, exigindo validação completa de autorização antes de qualquer uso.

O servidor deverá obrigatoriamente:

1. identificar o usuário autenticado;
2. determinar os tenants aos quais esse usuário tem acesso legítimo;
3. verificar se o tenant solicitado pertence a esse conjunto;
4. rejeitar a requisição quando não pertencer;
5. aplicar o tenant validado em todas as consultas subsequentes.

Nunca deverá existir consulta de domínio filtrada por um `tenantId` recebido do cliente sem essa validação.

Quando o acesso ocorrer em modo suporte, o tenant efetivo deverá derivar da sessão de suporte registrada, e a operação deverá ser auditada com a identidade real do operador.

## 3.3 Nenhum token externo exposto

A API jamais deverá retornar, em qualquer resposta:

- access token ou refresh token do Conta Azul;
- credenciais de integração, mesmo parcialmente mascaradas;
- chaves de provedores de IA;
- segredos de sessão, banco, Redis ou storage.

Endpoints de integração deverão expor apenas informações operacionais, como status da conexão, empresa externa identificada, data da última sincronização bem-sucedida e resumo do último erro.

## 3.4 Validação obrigatória de toda entrada

Toda entrada deverá ser validada no servidor antes de qualquer uso: corpo, parâmetros de rota, query string e cabeçalhos relevantes.

Regras:

- campos desconhecidos não deverão ser aceitos silenciosamente em operações de escrita;
- tipos deverão ser validados e convertidos explicitamente;
- limites numéricos, tamanhos de texto e intervalos de data deverão ser verificados;
- valores validados deverão ser tipados para uso interno seguro.

Validação existente no frontend nunca substitui validação no servidor.

## 3.5 Autenticação obrigatória por padrão

Toda rota é autenticada por padrão.

Rotas públicas constituem exceção explícita e limitada, restrita a login, recuperação de senha, redefinição de senha, verificação de saúde e retorno de OAuth do backend.

Toda rota autenticada deverá adicionalmente verificar autorização, considerando perfil do usuário e tenant efetivo.

## 3.6 Formato de erro consistente

Toda resposta de erro deverá utilizar o mesmo formato conceitual, definido na seção 4.9.

Não deverão existir formatos distintos por módulo.

## 3.7 Nenhum vazamento de detalhe interno

Respostas de erro não deverão conter:

- stack trace;
- mensagem bruta de exceção de banco de dados;
- nome de tabela, coluna ou constraint;
- caminho de arquivo;
- consulta SQL;
- segredo, mesmo parcial.

O detalhe técnico pertence ao log estruturado, conforme documento 09.11.

## 3.8 Semântica correta dos métodos e status

Métodos HTTP e códigos de status deverão respeitar sua semântica padrão.

Não é permitido responder erro com status 200, tampouco utilizar `POST` para operações que representam leitura simples.

## 3.9 Paginação obrigatória em listagens

Toda listagem potencialmente extensa deverá ser paginada, com limite máximo imposto pelo servidor.

Nenhum endpoint deverá retornar coleção ilimitada de dados financeiros.

## 3.10 Valores monetários sem perda de precisão

Valores monetários deverão trafegar em formato que preserve exatidão decimal.

É proibido serializar valores financeiros de modo que dependam de aritmética de ponto flutuante binário para reconstrução do valor correto.

O formato adotado deverá ser único e consistente em toda a API.

## 3.11 Determinismo

Dado o mesmo tenant, o mesmo período e os mesmos filtros, um endpoint analítico deverá retornar o mesmo resultado enquanto os dados de origem não mudarem.

---

# 4. Padrões Aprovados

## 4.1 Estilo do contrato

A API adota estilo REST orientado a recursos sempre que o conceito for naturalmente um recurso.

Quando a operação representar um comando de domínio que não corresponde a um recurso, é permitido expor uma ação explícita sob o recurso relacionado.

Exemplos conceituais:

```
GET    /receivables
POST   /integrations/{integrationId}/sync
POST   /auth/login
```

Nas rotas administrativas, o identificador de empresa é aceito no caminho, sempre com validação completa de autorização:

```
PATCH  /admin/tenants/{tenantId}/branding
```

A ação deverá permanecer subordinada ao recurso e possuir nome verbal claro.

## 4.2 Nomenclatura de rotas

- caminhos em minúsculas;
- substantivos no plural para coleções;
- separação por hífen quando houver mais de uma palavra;
- nomenclatura conceitual em inglês, coerente com os documentos 03 e 09.5;
- sem extensão de arquivo e sem verbos em rotas de leitura.

## 4.3 Versionamento

O versionamento por prefixo de caminho deverá ser adotado quando houver necessidade real de coexistência de contratos.

Diretrizes:

- durante o MVP, com frontend e backend evoluindo juntos, o versionamento poderá ser omitido;
- quando adotado, o prefixo deverá ser aplicado de forma uniforme a toda a API;
- alterações compatíveis, como adição de campo opcional, não exigem nova versão;
- alterações incompatíveis exigem nova versão ou período documentado de convivência;
- remoção de campo deverá ser precedida de depreciação registrada.

## 4.4 Métodos HTTP

| Método | Uso |
|--------|-----|
| `GET` | Leitura, sem efeito colateral |
| `POST` | Criação ou execução de comando de domínio |
| `PUT` | Substituição completa de recurso |
| `PATCH` | Atualização parcial de recurso |
| `DELETE` | Remoção ou desativação, conforme política da entidade |

`GET` deverá ser sempre seguro e idempotente.

## 4.5 Códigos de status

| Código | Uso |
|--------|-----|
| 200 | Sucesso com corpo |
| 201 | Recurso criado |
| 202 | Processamento assíncrono aceito |
| 204 | Sucesso sem corpo |
| 400 | Requisição malformada |
| 401 | Ausência de autenticação válida |
| 403 | Autenticado, porém sem permissão sobre o recurso ou tenant |
| 404 | Recurso inexistente no contexto do tenant efetivo |
| 409 | Conflito de estado |
| 422 | Entrada sintaticamente válida, porém semanticamente inválida |
| 429 | Excesso de requisições |
| 500 | Falha inesperada do servidor |
| 503 | Dependência indisponível |

Diretriz de segurança: quando um recurso pertencer a outro tenant, a resposta preferencial é 404, evitando confirmar a existência do recurso a quem não deveria conhecê-lo. O uso de 403 fica reservado a situações em que o usuário legitimamente enxerga o recurso, mas não possui permissão para a operação.

## 4.6 Parâmetros de rota

Parâmetros de rota deverão identificar recursos, jamais transportar filtros.

Todo parâmetro deverá ser validado quanto ao formato e resolvido dentro do tenant efetivo.

## 4.7 Query string

A query string deverá concentrar filtros, período, ordenação e paginação.

Convenções aprovadas:

```
?startDate=2026-08-01
&endDate=2026-08-31
&status=overdue
&customerId=...
&sort=-dueDate
&limit=50
&cursor=...
```

Diretrizes:

- nomes em `camelCase`, coerentes com o documento 09.2;
- múltiplos valores para o mesmo filtro representados de forma consistente em toda a API;
- parâmetros desconhecidos deverão ser ignorados ou rejeitados, de acordo com política única aplicada uniformemente;
- período deverá ser sempre validado quanto à ordem e à amplitude máxima permitida.

## 4.8 Ordenação e paginação

Ordenação:

- parâmetro único `sort`;
- prefixo `-` indica ordem descendente;
- somente campos explicitamente permitidos por endpoint;
- ordenação sempre determinística, com critério de desempate estável.

Paginação:

- paginação por cursor é o padrão para listagens grandes;
- paginação por deslocamento é aceitável apenas para conjuntos pequenos e limitados;
- `limit` possui valor padrão e máximo definidos pelo servidor;
- metadados de paginação acompanham a resposta da coleção.

Formato aprovado para coleções:

```
{
  "data": [ ... ],
  "pagination": {
    "limit": 50,
    "nextCursor": "...",
    "hasMore": true
  }
}
```

## 4.9 Formato de erro

Formato conceitual único para toda a API:

```
{
  "error": {
    "code": "VALIDATION_ERROR",
    "message": "Período informado é inválido.",
    "details": [
      { "field": "endDate", "issue": "must_be_after_start_date" }
    ],
    "requestId": "..."
  }
}
```

Regras:

- `code` é estável, em maiúsculas, e destinado ao consumo programático;
- `message` é destinada a exibição ao usuário, redigida em português e livre de detalhe técnico;
- `details` é opcional e utilizada principalmente em erros de validação;
- `requestId` permite correlacionar a resposta com o log estruturado;
- o código HTTP deverá ser coerente com o `code` retornado.

Códigos iniciais aprovados:

`VALIDATION_ERROR`, `UNAUTHENTICATED`, `FORBIDDEN`, `NOT_FOUND`, `CONFLICT`, `RATE_LIMITED`, `INTEGRATION_UNAVAILABLE`, `SYNC_IN_PROGRESS`, `INTERNAL_ERROR`.

Novos códigos deverão ser adicionados apenas quando representarem situação realmente distinta para o cliente.

## 4.10 Formato de resposta de sucesso

Não é obrigatório envelopar toda resposta bem-sucedida.

Padrão aprovado:

- coleções retornam objeto com `data` e `pagination`, pois a paginação exige metadados;
- recurso único retorna diretamente o objeto do recurso;
- operações sem retorno relevante respondem 204 sem corpo;
- operações assíncronas respondem 202 com identificador do processamento e status inicial.

Envelope genérico do tipo `{ "success": true, "data": ... }` não deverá ser adotado sem benefício claro, por adicionar profundidade sem informação útil.

A escolha deverá ser consistente: recursos equivalentes respondem no mesmo formato em toda a API.

## 4.11 Convenções de payload

- campos em `camelCase`;
- datas e instantes em formato ISO 8601;
- instantes com fuso horário explícito;
- enumerações em valores estáveis, em inglês, coerentes com o documento 09.5;
- ausência de valor representada como `null` explícito quando o campo fizer parte do contrato;
- nenhum campo interno de infraestrutura exposto.

## 4.12 Autenticação e contexto

A autenticação deverá ser verificada antes de qualquer lógica de domínio.

O contexto derivado da requisição deverá conter, no mínimo:

- usuário autenticado;
- perfil ou tipo de usuário;
- tenant efetivo já validado;
- indicação de modo suporte, quando aplicável;
- identificador de correlação da requisição.

Esse contexto deverá ser propagado às camadas internas em vez de reconstruído em cada serviço.

## 4.13 Autorização

A autorização deverá ser avaliada em duas dimensões:

1. o usuário pode executar esta operação, considerando seu perfil;
2. o usuário pode executá-la sobre este tenant e sobre este recurso.

Ambas as verificações são obrigatórias.

Verificar apenas o perfil, sem verificar o vínculo com o recurso, é falha de segurança.

## 4.14 Idempotência

Deverão ser naturalmente idempotentes: `GET`, `PUT`, `DELETE`.

Para operações `POST` sensíveis a repetição, deverá ser adotada estratégia de idempotência quando houver risco real de duplicidade, tais como:

- disparo manual de sincronização;
- solicitação de geração de relatório;
- operações administrativas críticas.

Padrões aprovados:

- chave de idempotência informada pelo cliente e associada ao tenant;
- reaproveitamento do resultado da primeira execução dentro da janela definida;
- retorno de conflito quando já existir execução equivalente em andamento;
- deduplicação natural apoiada em estado do domínio, quando suficiente.

## 4.15 Operações assíncronas

Operações longas deverão ser delegadas às filas, conforme ADR-010 e ADR-011.

Padrão aprovado:

- resposta 202 com identificador do processamento;
- endpoint de consulta de status;
- estados explícitos de processamento, sucesso e falha;
- ausência de bloqueio da requisição HTTP aguardando conclusão.

## 4.16 Limites e proteção

Padrões aprovados:

- limite de tamanho de corpo da requisição;
- limite de requisições por sessão e por origem em rotas sensíveis;
- limite máximo de intervalo de período em consultas analíticas;
- limite máximo de itens por página;
- tempo limite para dependências externas.

## 4.17 Saúde e diagnóstico

A API deverá expor endpoint de verificação de saúde.

Ele não deverá expor versões detalhadas de componentes, variáveis de ambiente, informações de infraestrutura nem dados de tenants.

---

# 5. Padrões Proibidos

É proibido:

1. utilizar `tenantId` recebido do cliente sem validar o vínculo com a sessão autenticada;
2. retornar tokens ou credenciais do Conta Azul em qualquer resposta;
3. expor chaves de IA, segredos de sessão ou credenciais de infraestrutura;
4. confiar em validação realizada no frontend;
5. responder erro com status 200;
6. utilizar formatos de erro distintos entre módulos;
7. retornar stack trace, SQL, nome de tabela ou caminho de arquivo ao cliente;
8. expor rota autenticada sem verificação de autorização sobre o recurso;
9. retornar listagem sem paginação;
10. permitir intervalo de período ilimitado em consultas analíticas;
11. utilizar `GET` com efeito colateral;
12. utilizar `POST` para leitura simples apenas para acomodar parâmetros extensos;
13. serializar valor monetário com perda de precisão;
14. criar endpoint que devolva dados de mais de um tenant sem que a operação seja explicitamente administrativa e devidamente autorizada;
15. duplicar regra de negócio entre rotas em vez de concentrá-la em serviço;
16. executar processamento pesado dentro do ciclo da requisição;
17. adicionar envelope genérico de sucesso sem benefício demonstrado;
18. alterar contrato existente de forma incompatível sem versionamento ou depreciação registrada;
19. expor endpoint de proxy que repasse chamadas diretamente ao Conta Azul;
20. registrar em log corpo de requisição contendo credenciais ou dados sensíveis.

---

# 6. Exemplos Conceituais

## 6.1 Resolução correta de tenant

Correto:

```
requisição chega
  ↓
sessão autenticada identificada
  ↓
tenants permitidos ao usuário resolvidos no servidor
  ↓
tenant solicitado verificado contra essa lista
  ↓
consulta executada com o tenant validado
```

Incorreto:

```
requisição chega
  ↓
tenantId lido do parâmetro
  ↓
consulta executada diretamente
```

## 6.2 Listagem paginada

Requisição:

```
GET /receivables?startDate=2026-08-01&endDate=2026-08-31&status=overdue&sort=-dueDate&limit=50
```

Resposta:

```
{
  "data": [
    {
      "id": "...",
      "description": "...",
      "dueDate": "2026-08-12",
      "originalAmount": "1250.00",
      "openAmount": "1250.00",
      "status": "overdue",
      "customer": { "id": "...", "name": "..." }
    }
  ],
  "pagination": { "limit": 50, "nextCursor": "...", "hasMore": true }
}
```

## 6.3 Recurso único

```
GET /integrations/{integrationId}
```

```
{
  "id": "...",
  "provider": "conta_azul",
  "status": "connected",
  "externalCompanyName": "...",
  "lastSuccessfulSyncAt": "2026-08-12T18:40:00-03:00",
  "lastErrorAt": null
}
```

Nenhum token é exposto.

## 6.4 Erro de validação

```
HTTP 422

{
  "error": {
    "code": "VALIDATION_ERROR",
    "message": "A data final deve ser posterior à data inicial.",
    "details": [ { "field": "endDate", "issue": "must_be_after_start_date" } ],
    "requestId": "..."
  }
}
```

## 6.5 Recurso de outro tenant

```
HTTP 404

{
  "error": {
    "code": "NOT_FOUND",
    "message": "Registro não encontrado.",
    "requestId": "..."
  }
}
```

A resposta não revela que o recurso existe em outra empresa.

## 6.6 Operação assíncrona

Exemplo de padrão (não é o contrato da F12 V1). Relatórios V1 são
`GET /reports/revenue` e `GET /reports/expenses` síncronos — ver §17.

```
POST /reports
Idempotency-Key: ...

HTTP 202

{
  "reportId": "...",
  "status": "processing"
}
```

Consulta posterior:

```
GET /reports/{reportId}
```

## 6.7 Erro interno

Ao cliente:

```
HTTP 500

{
  "error": {
    "code": "INTERNAL_ERROR",
    "message": "Não foi possível concluir a operação. Tente novamente.",
    "requestId": "..."
  }
}
```

No log estruturado permanece o detalhe técnico completo, correlacionado pelo mesmo `requestId`.

---

# 7. Critérios de Revisão

Um endpoint somente poderá ser aprovado quando:

1. derivar o tenant efetivo da sessão autenticada e validar qualquer identificador recebido;
2. verificar autenticação e autorização antes de qualquer lógica de domínio;
3. validar integralmente corpo, parâmetros e query string;
4. utilizar método HTTP e código de status semanticamente corretos;
5. utilizar o formato de erro único definido neste documento;
6. não vazar detalhe técnico interno em respostas;
7. não expor tokens, credenciais ou segredos;
8. paginar toda listagem potencialmente extensa e impor limite máximo;
9. permitir ordenação apenas em campos explicitamente autorizados;
10. limitar a amplitude de período em consultas analíticas;
11. preservar exatidão decimal em valores monetários;
12. delegar processamento pesado às filas e responder 202 quando aplicável;
13. adotar idempotência quando houver risco real de duplicidade;
14. manter consistência de formato com endpoints equivalentes já existentes;
15. concentrar regra de negócio em serviço, mantendo a rota fina;
16. registrar log estruturado correlacionado por identificador de requisição;
17. auditar operações administrativas e ações executadas em modo suporte;
18. permanecer compatível ou possuir estratégia explícita de versionamento e depreciação.

---

# 8. Relação com Outros Documentos

| Documento | Relação |
|-----------|---------|
| 09.3 — Backend | Define camadas, serviços e organização interna que sustentam estes endpoints |
| 09.7 — Autenticação | Define autenticação, sessão, recuperação de acesso e proteção de endpoints |
| 09.8 — Segurança | Define proteção de segredos, superfície de ataque e princípios deny-by-default |
| 09.9 — Multiempresa | Define o isolamento por tenant e a resolução do contexto de empresa |
| 09.10 — Tratamento de Erros | Define a taxonomia autoritativa de erros e o que chega ao cliente |
| 09.11 — Logs | Define o log estruturado correlacionado pelo identificador de requisição |
| 09.17 — Testes | Define a estratégia de testes de contrato, autorização e isolamento por tenant |
| 09.4 — Frontend | Define o consumidor oficial deste contrato |
| 09.5 — Banco de Dados | Define a persistência e as convenções de nomenclatura refletidas no contrato |
| 04 — API Conta Azul | Descreve a integração externa, acessível exclusivamente pelo backend |

---

# 9. Diretriz Final

A API interna do Dashboard Economização é a fronteira de confiança da plataforma.

Tudo que vem do cliente é solicitação.

Tudo que sai do servidor é decisão.

Nenhum identificador de empresa recebido do frontend poderá substituir a verificação de acesso feita no servidor, e nenhuma credencial de fornecedor externo poderá atravessar essa fronteira.

Contrato previsível, isolamento verificado e erro consistente valem mais para este produto do que qualquer sofisticação adicional de formato.

---

# 10. GET /dashboard/overview (Fase 10A)

Status: CONCLUÍDA.

Rota autenticada (`requireAuthentication`). Sem prefixo `/api/v1`.

Tenant operacional: exclusivamente da sessão (USER → `auth.tenantId`) ou do Support Mode ativo. Query `tenantId` é rejeitada (400). ADMIN/SUPER_ADMIN sem Support Mode → 403.

Não há body. Header de sucesso: `Cache-Control: private, no-store`.

Resposta 200 (valores financeiros em string via `Decimal.toString()`; fórmulas em docs/11, não duplicadas aqui):

- `today`: `YYYY-MM-DD` (mesma data civil `America/Sao_Paulo` do snapshot analítico)
- `receivables` / `payables`: `open`, `overdue`, `upcoming`
- `delinquency`: `overdueUnpaid`, `openUnpaid`, `rate` (`string | null`)
- `integration`: `status` (`CONNECTED | DISCONNECTED | ERROR`), `lastSuccessfulSyncAt` (ISO-8601 ou null), `lastErrorCode` (código público ou null)

Ausência de Integration Conta Azul: `DISCONNECTED` + freshness null. Disconnect/ERROR não apagam KPIs persistidos. UI (10B/10C) não faz parte deste contrato.

⸻

# 11. GET /dashboard/upcoming e GET /dashboard/cash-flow-forecast (Fase 10C)

Status: IMPLEMENTADA / HOMOLOGADA VISUALMENTE (10C). E1 (`summary`) IMPLEMENTADA / AGUARDANDO HOMOLOGAÇÃO.

Mesma autenticação e tenant operacional de §10. Query `tenantId` rejeitada (400).
Header de sucesso: `Cache-Control: private, no-store`. Decimais em string.
Fórmulas: docs/11 (motor 9C). Sem bloco `integration` (freshness permanece em overview).

## GET /dashboard/upcoming?days=N

`days` obrigatório. Aceitos somente `7`, `15` e `30` (contrato de produto da Dashboard).
Ausente ou outro valor → 400. Sem default no backend. O motor analítico continua
sem default implícito (`nDays` informado pelo caller).

Resposta 200:

- `today`, `from`, `to`: `YYYY-MM-DD`
- `nDays`: 7 | 15 | 30
- `summary`: `receivable`, `payable`, `net` (strings decimais)
  (`net` = soma dos `unpaid` a receber − soma dos `unpaid` a pagar
  na mesma janela; não é saldo bancário)
- `receivables.items` / `payables.items`: `id`, `dueDate`, `unpaid`, `status`
  (`OPEN | OVERDUE | PARTIALLY_PAID`)

Sem `tenantId`, Party, descrição ou payload upstream.

## GET /dashboard/cash-flow-forecast

Sem query de horizonte. `horizonDays` ecoa o motor (90).

Resposta 200:

- `today`, `from`, `to`: `YYYY-MM-DD`
- `horizonDays`: 90
- `buckets[]`: `key` (`YYYY-MM`), `inflows`, `outflows`, `net` (strings)

`net` é o líquido do bucket, não saldo acumulado.

⸻

# 12. GET /dashboard/expense-composition (E2)

Status: IMPLEMENTADA (estoque AP em aberto). **Não** alimenta a Home após P1.1.
Contrato preservado para carteira / drill-down futuro. A Home usa §12d.

Mesma autenticação e tenant operacional de §10. Query `tenantId` rejeitada (400).
Header de sucesso: `Cache-Control: private, no-store`. Decimais em string.
Fórmula: docs/11 §10 (D8). Estoque de AP em aberto; não é realizado.

Resposta 200:

- `today`: `YYYY-MM-DD`
- `payables.total`, `classified`, `uncategorized`, `imprecise`: strings
- `payables.coverageRate`: string × 100 ou `null` se total zero
- `payables.items[]`: `kind` (`category | uncategorized | imprecise`;
  `other` só tolerado em payloads legados — o backend novo não produz),
  `name`, `amount`, `percentage` (participação sobre `total`)

Sem `categoryId`, `tenantId`, Party ou payload upstream.
Composições retornam **todas** as categorias nominais disponíveis (sem Top N
semântico). Buckets de qualidade (`uncategorized` / `imprecise`) permanecem.
Reduções Top N são exclusivas de componentes visuais compactos na UI e não
alteram o dataset financeiro.

⸻

# 12b. GET /dashboard/receivable-composition

Status: IMPLEMENTADA (estoque AR em aberto). A Home deixou de consumir este
contrato; a visão mensal é §12c.

# 12c. GET /dashboard/monthly-revenue

Status: HOMOLOGADA (M1: query `month`) — **API legada de competência**.

**Estado atual (CASH-4 / CASH-6 homologados):** a Home oficial **não** consome
este endpoint. KPIs oficiais usam `GET /dashboard/monthly-cash-flow`
(regime de caixa). Este contrato permanece disponível para compatibilidade
técnica e clientes/testes que ainda o chamem; **não** é o eixo dos números
oficiais do produto.

Mesma autenticação e tenant operacional de §10. Query `tenantId` rejeitada (400).
Header de sucesso: `Cache-Control: private, no-store`. Decimais em string.

Query opcional:

- `month=YYYY-MM` — mês civil de competência (`America/Sao_Paulo`). Ausente = mês corrente.
  Formato estrito; inválido → 400. Não altera fórmulas.

Receitas de competência no mês solicitado. Inclui `OPEN | OVERDUE | PARTIALLY_PAID | PAID`.
Exclui `RENEGOTIATED`. `competenceDate` nula não entra. `dueDate` não escolhe o mês.
`received` = Σ `paid` do snapshot (liquidado da receita do mês), **não** caixa do mês.
Em mês passado, `received`/`outstanding`/`overdue` refletem o **estado atual** dos títulos
cuja competência pertence ao mês — não fechamento histórico (sem as-of).
Fórmula: docs/11. Não é faturamento.

Resposta 200:

- `today`, `from`, `to`: `YYYY-MM-DD`
- `monthKey`: `YYYY-MM`
- `receivables.total`, `received`, `outstanding`, `overdue`
- `receivables.classified`, `uncategorized`, `imprecise`, `coverageRate`
- `receivables.items[]`: `kind`, `name`, `amount` (total), `received`, `outstanding`, `percentage`
- `receivables.daily[]`: `{ date, amount, received, outstanding }` — série diária
  por **competenceDate**; `amount` = Σ `total` dos títulos com competência naquele
  dia civil. Preenche todos os dias do mês (zero honesto sem título). **Não é caixa.**
  Não usa `paid`/`dueDate` como eixo temporal.
  `received` = Σ `paid` e `outstanding` = Σ `unpaid` dos títulos daquele dia de
  competência — **snapshot do estado atual**, não o que entrou em caixa no dia.
  Leitura correta: “valor atualmente recebido dos títulos com competência neste
  dia”; leitura proibida: “recebido neste dia”.

Sem `categoryId`, `tenantId`, Party ou payload upstream.

Nota CASH-3B / CASH-4 / CASH-6 (estado atual homologado):
`GET /dashboard/monthly-cash-flow` é a fonte oficial da Home.
`billing` = `monthlyBilling` = realized.inflows + expected.receivables.
Vencido não entra. Pagamento tardio no mês da baixa (`occurredOn` / netAmount).
Query: `month`, `costCenter`, `category`. `situation` se enviada é validada e
**não** filtra o realizado. `tenantId` rejeitado (400). Monetário em string.
`costCenterCashSplit=false` preserva null.
`realizedByCategory.*.items[]` inclui `key` estável (`externalId` ou
`uncategorized`/`imprecise`) para drill-down via `GET /dashboard/cash-realized/details`.
Competência (§12c / §12d) permanece nos endpoints legados; não alimenta KPIs oficiais.

### GET /dashboard/cash-movement-history (Correção 08-B)

Status: IMPLEMENTADA — histórico de **12 meses** de caixa **realizado** para o
modo Mensal da Movimentação financeira na Home.

Query: `month` (YYYY-MM do **mês final** da janela; ausente = mês civil corrente),
`costCenter`, `category`. `situation` se enviada é validada e ignorada.
`tenantId` rejeitado (400). Sem parâmetro `months` (sempre 12).

Janela: `endMonth = month` selecionado; `startMonth = endMonth − 11`;
12 buckets cronológicos (mais antigo → mais recente).

Cálculo: o backend chama o **mesmo** `MonthlyCashFlowService` para cada
`monthKey` e serializa somente `realized.{inflows,outflows,result}`.
Meses sem movimento: `"0"`. Indisponibilidade de cost-center split: `null`
(nunca zero). `costCenterCashSplit` global = false se qualquer mês da janela
estiver unavailable.

Não inclui expected, daily, categorias, nem saldo bancário.
Header: `Cache-Control: private, no-store`.
Home: lazy — só ao ativar o modo Mensal.

### GET /dashboard/cash-expected-horizon

Status: IMPLEMENTADA — **Previsto multi-mês à frente** para Movimentação financeira
(Diária → Previsto → Trimestral/Semestral/Anual).

Query:
- `month` (YYYY-MM **âncora**; ausente = mês civil corrente SP);
- `horizon` **obrigatório**: `3` | `6` | `12` (outros → 400);
- `costCenter`, `category` opcionais;
- `situation` se enviada é validada e ignorada;
- `tenantId` rejeitado (400).

Janela móvel: `startMonth = month`; `endMonth = month + (horizon − 1)` meses civis
(ex.: `2026-09` + `horizon=6` → SET/2026…FEV/2027). Cruza ano via calendário civil.

Cálculo: **uma** carga de receivables/payables (ou alocações CURRENT se CC) +
`selectExpectedOpenReceivables` / `selectExpectedOpenPayables` por bucket
(mesma regra CASH-3B do `monthly-cash-flow.expected`). **Sem** ledger/realizado.
**Sem** N× `MonthlyCashFlowService`.

Resposta (money em string decimal | null):
- `today`, `startMonth`, `endMonth`, `horizon`, `costCenterCashSplit`;
- `totals.{receivables,payables,result}`;
- `months[]` com `monthKey` + `expected.{receivables,payables,result}`
  (meses sem movimento = `"0"`; CC unavailable = `null`).

Invariantes: Σ buckets.receivables = totals.receivables;
Σ buckets.payables = totals.payables;
`result = receivables − payables` (por mês e no total).

“Mês atual” (horizon 1) **não** usa este endpoint — continua em
`GET /dashboard/monthly-cash-flow` + série diária.
Header: `Cache-Control: private, no-store`.

### GET /dashboard/cash-balance-history (Correção 08-C2)

Status: IMPLEMENTADA — fundação de **saldo bancário real** via snapshots
(`saldo-atual`). Separado de `cash-movement-history` (semântica distinta).

Query: `month` (YYYY-MM do mês diário / fim da janela mensal; ausente = mês
civil corrente). **Não** aceita `category`, `costCenter` nem `tenantId`
(400). Tenant = contexto operacional.

Resposta (money em string decimal):

* `today`, `availableFrom` | null, `availableTo` | null;
* `pointCount`, `accountsIncluded`;
* `coverage`: `none` | `partial` | `available`;
* `daily[]`: `{ date, balance }` — fechamento do dia civil SP;
* `monthly[]`: `{ monthKey, balance }` — último consolidado do mês
  (janela de 12 meses terminando em `month`).

Regras:

* fonte = snapshots reais; **não** ledger;
* `availableFrom` = primeiro dia consolidado publicado (metadata); não gate
  prévio por cohort de ativas atuais;
* carry-forward só após o primeiro snapshot da conta participante;
* consolidado = soma das contas **participantes do dia** com cobertura
  completa (nunca soma incompleta; gaps ≠ zero);
* participação no dia D (11-B): `firstSnapshot <= D` e
  (`active=true` **ou** `D <= lastSnapshot`);
* conta que ainda não chegou ao `firstSnapshot` não participa e não invalida D;
* conta inativa preserva histórico até o último snapshot; não carrega
  depois; não inventa saldo antes do primeiro;
* `active` controla captura futura de `saldo-atual`, não apaga snapshots;
* `accountsIncluded` = contas distintas na janela diária do mês selecionado;
* `accountActiveAtCapture` permanece no schema (captura atual sempre em
  contas ativas); não é o mecanismo principal do cohort histórico.

Header: `Cache-Control: private, no-store`.
Linha no gráfico Home = 08-C3/C4 (ainda não entregue).

### GET /dashboard/receivables/expected-details (CASH-4 receivable detail)

Status: IMPLEMENTADA — detalhe **lazy** do KPI `expected.receivables`.

Semântica idêntica ao bloco `expected.receivables` de `GET /dashboard/monthly-cash-flow`
(mesmos filtros `month`, `costCenter`, `category`; vencidos fora; `tenantId` rejeitado).
Resposta: `total` reconciliável com o KPI + `items[]` (`dueDate`, `amount`, `description`,
`customerName` via `Party`, `categoryNames` resolvidas localmente).
`available=false` quando o share por centro de custo é indisponível (mesma regra do KPI).
Header: `Cache-Control: private, no-store`. Carregado sob demanda ao abrir o modal “A receber”.

### GET /dashboard/payables/expected-details (CASH-5 payable detail)

Status: IMPLEMENTADA — detalhe **lazy** do KPI `expected.payables` (Contas a pagar).

Semântica idêntica ao bloco `expected.payables` de `GET /dashboard/monthly-cash-flow`
(mesmos filtros `month`, `costCenter`, `category`; vencidos fora; `tenantId` rejeitado).
Resposta: `total` reconciliável com o KPI + `items[]` (`dueDate`, `amount`, `description`,
`supplierName` via `Party`, `categoryNames` resolvidas localmente).
`available=false` quando o share por centro de custo é indisponível (mesma regra do KPI).
Header: `Cache-Control: private, no-store`. Carregado sob demanda ao abrir o modal “Contas a pagar”.

### GET /dashboard/cash-realized/details (12-B)

Status: IMPLEMENTADA — detalhe **lazy** das baixas realizadas por `categoryKey`.

Explica `realizedByCategory.inflows|outflows.items[key]` do `GET /dashboard/monthly-cash-flow`
sob os mesmos filtros (`month`, `costCenter`, `category`; `tenantId` rejeitado).

Query obrigatória:
- `direction=inflows|outflows`
- `categoryKey` — identidade estável exposta em `realizedByCategory.*.items[].key`
  (`externalId` nominal, `uncategorized` ou `imprecise`)
- `categoryKind` (opcional, recomendado) — `category|other|uncategorized|imprecise`;
  quando presente, filtra **kind+key** e elimina colisão residual se um
  `externalId` literal coincidir com bucket especial.

Query opcional: `month`, `costCenter`, `category`, `limit` (default 100, máx. 200), `offset` (default 0).

Autoridade monetária = `financial_transactions` (ACTIVE, `financialTransferId` null, `occurredOn` no mês).
AR/AP + Party = metadata histórica (join por `installmentExternalId`, **sem** exigir installment ACTIVE).
Com filtro de centro: share via `findHistorical*AllocationsByExternalIds` (11-E.3).

Resposta: `available`, `total` (= Σ `attributedAmount` de **todos** os itens da categoria),
`itemCount`, `limit`, `offset`, `categoryKind` (eco da query ou null), `items[]` da página
(`settlementExternalId`, `installmentExternalId`, `occurredOn`, `netAmount`, `attributedAmount`,
`description`, `partyName`, `categoryNames`, `categoryExternalIds`, `categoryKey`…).

`SUM(attributedAmount)` reconcilia com o amount do pai; paginação não altera `total`.
Header: `Cache-Control: private, no-store`.

⸻

# 12d. GET /dashboard/monthly-expenses

Status: IMPLEMENTADA (P1.1) — **API legada de competência** (espelho de §12c).
Home oficial usa caixa (`monthly-cash-flow`); este endpoint não alimenta KPIs oficiais.

Espelho de §12c no lado AP. Query `month=YYYY-MM` opcional; `tenantId` rejeitada (400).
Header: `Cache-Control: private, no-store`.

Despesas de competência no mês. Inclui `OPEN | OVERDUE | PARTIALLY_PAID | PAID`.
Exclui `RENEGOTIATED`. `competenceDate` nula não entra. `dueDate` não escolhe o mês.
`payables.paid` HTTP = Σ `paid` do snapshot, **não** caixa do mês.
Composição D8 sobre o **total** do mês (tipo esperado EXPENSE).

Resposta 200:

- `today`, `from`, `to`, `monthKey`
- `payables.total`, `paid`, `outstanding`, `overdue`
- `payables.classified`, `uncategorized`, `imprecise`, `coverageRate`
- `payables.items[]`: `kind`, `name`, `amount`, `paid`, `outstanding`, `percentage`
- `payables.daily[]`: `{ date, amount, received, outstanding }` — série diária por
  **competenceDate**; `amount` = Σ `total`. **Não é caixa.**
  `received` = Σ `paid` (pago) e `outstanding` = Σ `unpaid` (a pagar) dos títulos
  daquele dia de competência — **snapshot do estado atual**, não caixa do dia.
  A chave permanece `received` no ponto diário, espelhando §12c; em
  `payables.items[]` o mesmo conceito aparece como `paid`.

⸻

# 13. GET /dashboard/month-end-cash-pressure (P2)

Status: IMPLEMENTADA / AGUARDANDO HOMOLOGAÇÃO.

Mesma autenticação e tenant operacional de §10. Query `tenantId` rejeitada (400).
Header: `Cache-Control: private, no-store`. Decimais em string.

Agenda restante do mês civil atual (today-anchored). Não segue `?month=`.

Recorte: `from = today` civil, `to = último dia do mês civil`, timezone America/Sao_Paulo.
Títulos ACTIVE unpaid com `dueDate ∈ [from, to]`. Exclui vencidos antes de hoje.

Resposta 200:

- `today`, `monthKey`, `from`, `to`
- `summary.receivable`, `summary.payable`, `summary.net` (strings; `net = receivable − payable`)

Sem `tenantId`, Party, descrição ou payload upstream.
`net` não é saldo bancário.

⸻

# 14. GET /dashboard/executive-insights (E3 mensual — P2)

Status: IMPLEMENTADA / AGUARDANDO HOMOLOGAÇÃO.

Mesma autenticação e tenant operacional de §10. Query `tenantId` rejeitada (400).
Query opcional `month=YYYY-MM` (espelha §12 monthly-revenue).
Header: `Cache-Control: private, no-store`. Decimais em string.
Insights determinísticos da competência selecionada. Não é IA.

Resposta 200:

- `today`, `monthKey`, `from`, `to`
- `insights[]`: `{ id, body }`
  - ids: `revenue-expense-total`, `revenue-expense-balance`, `top-revenue-category`,
    `top-expense-category`, `expense-classification-gap`
  - máximo 4 itens; mês vazio → `insights: []`

Sem `tenantId`, Party, descrição ou payload upstream.
Textos prontos no backend; frontend não recalcula.

Fórmulas mensais: docs/11 (competência) sem alteração silenciosa.

⸻

# 15. Meta de faturamento (F2)

Status: **HOMOLOGADA**.

Persistência: **SIM** — tabela `revenue_goals` (uma linha por `tenantId + monthKey`,
ver docs/03 e docs/09.5). Por tenant: **SIM**. Por competência: **SIM**.
Cadastro e edição via `PUT`. Histórico de competências no `GET`. Meta × Realizado,
falta e excesso calculados no backend. Competência futura com meta: `PLANNED`.
O realizado **não** é persistido: vem de `MonthlyCashFlow.billing`
(`realized.inflows + expected.receivables`, CASH-4B homologada).
O endpoint legado `GET /dashboard/monthly-revenue` **não** define o
`actual` da meta oficial.
IA para metas / sugestão automática: FUTURA / NÃO IMPLEMENTADA.
Gráfico histórico Meta × Realizado: melhoria futura (fora da F2 homologada).

Autenticação e tenant operacional iguais a §10 / §12c: sessão ou Support Mode.
ADMIN/SUPER_ADMIN **sem** Support Mode recebe 403 na leitura e na gravação.
`tenantId` nunca é aceito — nem em query, nem no corpo (400).

## `GET /dashboard/revenue-goal?month=YYYY-MM`

Query `month` opcional (mês civil SP); ausente = mês corrente.
`Cache-Control: private, no-store`.

Resposta 200:

- `monthKey`: mês civil resolvido
- `target`: decimal-string | `null` (meta não configurada)
- `actual`: decimal-string — `MonthlyCashFlow.billing` (caixa, company-level)
- `achievementRate`: decimal-string × 100 | `null` (mesmo contrato das demais taxas)
- `remaining`: `max(target − actual, 0)` | `null`
- `exceeded`: `max(actual − target, 0)` | `null`
- `status`: `NO_TARGET` | `IN_PROGRESS` | `NOT_ACHIEVED` | `ACHIEVED` |
  `EXCEEDED` | `PLANNED`
- `history[]`: 6 competências terminando na selecionada, da mais antiga para a
  mais recente — `{ monthKey, target, actual, achievementRate, status }`

Semântica temporal do `status` (F2.0.1), relativa à competência civil corrente
(America/Sao_Paulo):

- sem meta → `NO_TARGET`
- mês **atual** abaixo → `IN_PROGRESS`
- mês **passado** abaixo → `NOT_ACHIEVED` (competência encerrada não fica
  “em andamento”)
- mês **futuro** com meta → `PLANNED` (não julga atingimento, mesmo com títulos
  já lançados acima/abaixo da meta)
- `actual == target` → `ACHIEVED`; `actual > target` → `EXCEEDED` (só em
  competências corrente ou encerrada)

Fórmulas (`achievementRate`, `remaining`, `exceeded`) permanecem inalteradas;
só a interpretação do status muda.

## `PUT /dashboard/revenue-goal`

Corpo: `{ "month": "YYYY-MM", "target": "180000.00" }`.

- `month` é **obrigatório e explícito** — a gravação nunca infere a competência
- `target` só aceita decimal-string positivo com até 4 casas; `0`, negativo,
  `NaN` e número em ponto flutuante retornam 400 e **nada** é persistido
- upsert por `tenantId + monthKey`: apenas o último valor é guardado
  (não há tabela de revisões da meta)

Resposta 200: mesmo contrato do `GET` para a competência gravada.

Derivados são calculados no backend (`calculateRevenueGoalProgress`); o frontend
apenas formata. IA / sugestão de meta: fora deste contrato.

---

# 16. Centros de custo (CC1)

## `GET /dashboard/cost-centers`

Lista centros de custo do tenant para o seletor (id, name, code, active).
Autenticação/tenant iguais às demais rotas de dashboard.

Query de período (mesmo parser da listagem de categorias — 11-A.1 / 11-C):

- `month=YYYY-MM` — Home; contexto `dashboard_month`.
- `from=YYYY-MM&to=YYYY-MM` — Relatórios; contexto `reports_range`.
- Ausente — mês civil atual (`America/Sao_Paulo`) como `dashboard_month`.

Visibilidade:

- Home mês atual ou futuro → somente `active=true`.
- Home mês passado → `active=true` OU centro usado no período (competence/due/settlement).
- Relatórios (`from`/`to`) → sempre visão histórica no intervalo.

## Query `costCenter` nos GETs de dashboard

Query opcional `costCenter=<uuid>` (CostCenter.id) filtra KPIs/composições/
monthly-revenue/monthly-expenses/insights/upcoming/forecast/month-end pelo
rateio oficial (`allocation.amount`). Ausente = visão consolidada da empresa.

`GET`/`PUT /dashboard/revenue-goal` **ignoram** `costCenter`, `situation` e
`category` — a meta permanece sempre consolidada da empresa.

## URL state da Home (F11-A / F11-B1)

Query params oficiais (código vigente):

- `month=YYYY-MM` — mês civil de competência (`America/Sao_Paulo`). Ausente = mês corrente.
- `costCenter=<uuid>` — centro de custo. Ausente = consolidado (Todos).
- `situation=settled|open|overdue` — situação da Home. Ausente = Todas.
  Inválido → 400. Não usar `status`. `overdue` segue D1, não `status=OVERDUE`.
- `category=<uuid>` — `FinancialCategory.id` do tenant. Ausente = Todas.
  UUID malformado → 400. Inexistente / outro tenant → 404.

Não são query params da Home: `period`, `comparison`, `costCenterId`, `status`
(`costCenterId` é identificador interno).

## GET /dashboard/categories

Catálogo do tenant autenticado para o picker da Home / Relatórios.
Sem `tenantId` na query (400). Payload: `{ items: [{ id, name, type }] }`.
Ordenação `active desc`, `type`, `name`, `id`. Não expõe `externalId` nem `active`.
Catálogo vazio → `items: []`.

Query de período (reutiliza o parser de cost-centers):

- `month=YYYY-MM` — Home; contexto `dashboard_month`.
- `from=YYYY-MM&to=YYYY-MM` — Relatórios; contexto `reports_range`.
- Ausente — mês civil atual (`America/Sao_Paulo`) como `dashboard_month`.

Visibilidade (Correção 11-C; mesma filosofia 11-A.1):

- Home mês atual ou futuro → somente categorias `active=true`.
- Home mês passado → `active=true` OU categoria historicamente usada no período
  via `categoryExternalIds` em AR/AP (competenceDate, dueDate ou settlement
  cash ACTIVE com `financialTransferId=null` e `occurredOn` no período).
- Relatórios (`from`/`to`) → sempre visão histórica no intervalo.
- Chave histórica: `(integrationId, categoryExternalId)` — sem cruzar integrações.
- Lookups por id / externalIds (filtros, export, analytics) **não** filtram
  `active=true`; inactive continua resolvível.

## Situação e categoria nos endpoints mensais

`GET /dashboard/monthly-revenue`, `GET /dashboard/monthly-expenses` e
`GET /dashboard/executive-insights` aceitam `month`, `costCenter`, `situation`
e `category`. Todos os derivados (KPIs, daily, composição, overdue, insights)
saem do mesmo conjunto filtrado.

`GET /dashboard/cash-flow-forecast` e `GET /dashboard/month-end-cash-pressure`:
`category` aplica; `situation` é parseada (400 se inválida) e **não** altera o
universo (P1.1 / dueDate).

FILTER-001/005 (ranges) NÃO fazem parte do contrato da Home.
F11-B1 CONCLUÍDA. F11-B2 HOMOLOGADA (F11-B3 CONCLUÍDA, 24/08/2026): a Home
passa a enviar `situation` e `category` nos GETs mensais/insights (e na
comparação do mês anterior); forecast/pressão enviam `category` e não
`situation`; meta/overview/upcoming não fatiam por esses params.

Intervalo de meses civis de competência (Relatórios F12-A) NÃO entra nestes
GETs da Home. Contrato oficial: §17.

⸻

# 17. Relatórios (F12-A + CASH-6)

Status atual (PRE-F13-CASH-FINAL-AUDIT / CASH-6 homologado, commit `39e2ab3`):
Relatórios, PDF e XLSX usam **regime de caixa** via o mesmo
`MonthlyCashFlowService` da Home. Competência **não** define totais oficiais.

Histórico F12 (24–25/08/2026): F12-B/C/D implementaram a superfície
`/reports/revenue|expenses` + PDF/XLSX inicialmente sobre competência.
**SUPERSEDED por CASH-6** quanto ao motor financeiro. Os endpoints de
competência da Dashboard (§12c) permanecem como legado; a paridade oficial
atual é com `GET /dashboard/monthly-cash-flow`, não com monthly-revenue/expenses.

## 17.1 Recorte V1 (estado atual — caixa)

Tipos oficiais:

- `revenue` — Entradas de caixa (fonte: MonthlyCashFlow / realized + expected).
- `expenses` — Saídas de caixa (fonte: MonthlyCashFlow / realized + expected).

`situation` na query é aceito por compatibilidade de bookmark e **ignorado**
no domínio de caixa (não filtra o ledger realizado). Vencido aparece como
KPI separado (`overdue.ofMonth`), não como terceiro tipo de relatório.

Fora da V1 (não implementar nesta superfície):

- listagem transacional item a item do ledger;
- DRE contábil; NF-e; faturamento fiscal;
- taxa de inadimplência de estoque **variando com De/Até** (contradiz D2);
- upcoming / forecast / pressão (permanecem na Home);
- meta de faturamento (company-level; widget da Home);
- IA; construtor arbitrário; dashboard builder;
- link público; e-mail; agendamento;
- FILTER-001 em dias soltos e range diário FILTER-005.
- as-of histórico de unpaid.

A taxa `GET /dashboard/overview` → `delinquency` permanece snapshot de **hoje**
(D1/D2). Se um dia for exibida em Relatórios, o intervalo De/Até **não** a
altera. Não entra na F12-B.

## 17.2 Tempo

Timezone: `America/Sao_Paulo`.

Estado atual (CASH-6): eixo de caixa por **mês civil**.

- Realizado: `occurredOn` no mês
- Previsto: `dueDate` no mês e ainda no prazo (`unpaid`)
- Vencido: separado (`overdue.ofMonth` / D1)

Query:

- `from=YYYY-MM` — primeiro mês civil (inclusive, obrigatório);
- `to=YYYY-MM` — último mês civil (inclusive, obrigatório).

Exemplo: `from=2026-01&to=2026-08` = jan..ago/2026.

Regras: formato estrito YYYY-MM; `from > to` → 400; amplitude > 24 meses
civis → 400. Sem filtro diário na V1. Sem as-of histórico de unpaid.

Histórico F12 (pré-CASH-6): o eixo era `competenceDate` + snapshot `paid`/`unpaid`.
**SUPERSEDED** — não usar como contrato atual.

FILTER-001 (recorte mensal civil em intervalo) e FILTER-005 (De/Até de
meses, não dias) pousam nesta superfície. Não voltam para a Home.

## 17.3 Filtros

Além de `from`/`to`:

- `costCenter=<uuid>` — ausente = Todos (CC1);
- `category=<uuid>` — `FinancialCategory.id`; malformado 400; outro tenant 404;
- `situation=settled|open|overdue` — aceito por compatibilidade; **ignorado**
  no domínio de caixa (CASH-6).

`tenantId` em query ou corpo → 400. Tenant = sessão / overlay de Support Mode
(USER = próprio tenant; ADMIN/SUPER_ADMIN só com suporte ativo, senão 403
como o dashboard). Exportação PDF/XLSX usa o mesmo tenant da visualização.

## 17.4 Comparação

Fora da F12-B. A Home continua mês × mês civil anterior (dois GETs mensais).
Comparar intervalo × intervalo equivalente exigiria fórmula nova — não
inventar. D9 permanece para taxas (`null` se denominador 0).

## 17.5 Rotas V1 (síncronas)

Não usar o exemplo genérico `POST /reports` 202 (§6.6) como contrato da V1.

```
GET /reports/revenue?from=YYYY-MM&to=YYYY-MM
GET /reports/expenses?from=YYYY-MM&to=YYYY-MM
```

Query opcional: `costCenter`, `situation`, `category`.
Header de sucesso: `Cache-Control: private, no-store`.
Decimais em string.

Autenticação: `requireAuthentication` + `assertNoTenantIdQuery`, iguais ao
dashboard.

Agregação: o backend itera cada `monthKey` em `[from, to]` e chama o
**mesmo** `MonthlyCashFlowService` da Home (CASH-6). Totais do intervalo:

- revenue: `total` = Σ billing (`realized.inflows + expected.receivables`);
  `received` = Σ realized.inflows; `outstanding` = Σ expected.receivables;
  `overdue` = Σ overdue.ofMonth.receivables; composição = realizedByCategory.inflows
- expenses: `total` = Σ (outflows + expected.payables); `paid` = Σ outflows;
  `outstanding` = Σ expected.payables; `overdue` = Σ ofMonth.payables;
  composição = realizedByCategory.outflows

`coverageRate` = classificados ÷ realizado do intervalo (`null` se
realizado 0 ou split unavailable). `situation` é aceito na query por
compatibilidade e **ignorado** (não se aplica ao realizado histórico).
`months[]` preserva o detalhe por mês civil de caixa.

Proibido: o frontend somar N chamadas a `/dashboard/monthly-*` para montar
o intervalo (N+1, cache divergente, PDF inconsistente).

Resposta 200 (revenue; expenses espelha com `payables` / `paid`):

- `today`, `from`, `to` (`YYYY-MM`)
- `receivables.total|received|outstanding|overdue|classified|uncategorized|imprecise|coverageRate`
  (expenses: `payables.total|paid|outstanding|overdue|…`)
- `receivables.items[]` — mesmo shape de §12c, agregado no intervalo
  (expenses: items usam `paid` no lugar de `received`)
- `months[]`: `{ monthKey, receivables: { ... mesmo shape mensal de §12c } }`
  (expenses: `payables`, incluindo `daily` só no mês)

Sem `tenantId`, Party, payload upstream, ledger.

Erros: 401; 403 (plataforma sem suporte); 400 (mês, intervalo, situation,
`tenantId`); 404 (category/costCenter inexistente no tenant).

Síncrono na V1. Critério para job futuro (REPORT-005 / §4.15): intervalo
acima do teto de 24 meses **ou** exportação de listagem item a item que
estoure tempo HTTP. Até lá, não criar fila de relatórios.

## 17.6 Formatos (F12-C Receita / F12-D Despesas)

- Visualização na tela: SIM (F12-B Receita; F12-D Despesas).
- PDF e Excel/XLSX: SIM para Receita (F12-C) e Despesas (F12-D).
- Biblioteca: `pdfkit` (PDF) e `exceljs` (XLSX), geração **no backend**.
- Impressão: `window.print` / CSS print se suficiente; não obrigar PDF engine.
- CSV: não obrigatório (XLSX existe).
- Link público / e-mail / agendamento: NÃO (REPORT-004).
- Job / `POST /reports` 202: NÃO nesta fase (teto 24 meses, síncrono).

Transporte oficial:

```
GET /reports/revenue?from=YYYY-MM&to=YYYY-MM&format=pdf
GET /reports/revenue?from=YYYY-MM&to=YYYY-MM&format=xlsx
GET /reports/expenses?from=YYYY-MM&to=YYYY-MM&format=pdf
GET /reports/expenses?from=YYYY-MM&to=YYYY-MM&format=xlsx
```

Sem `format` (ou `format=json`): JSON. `format` inválido → 400.
Os mesmos parâmetros analíticos (`from`, `to`, `costCenter`, `category`).
`situation` ignorado. `tenantId` → 400. Mesma autenticação / Support Mode /
teto de 24 meses. Exporters **formatam** o resultado de `getRevenueReport` /
`getExpensesReport` (caixa); não recalculam fórmulas. Cabeçalho:
“Relatório financeiro — Regime de caixa”.

PDF: `Content-Type: application/pdf`. XLSX: `Content-Type:
application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`.
`Content-Disposition: attachment; filename="relatorio-receita-{from}-a-{to}.{pdf|xlsx}"`
ou `relatorio-despesas-{from}-a-{to}.{pdf|xlsx}`.
`Cache-Control: private, no-store`. Texto de categoria/empresa/filtro
que comece com `= + - @` é neutralizado no XLSX (formula injection).
`coverageRate` null permanece estado neutro (não 0%). Sem UUID/tenantId
no artefato. Relatório vazio gera documento válido informando ausência
de dados.

PDF profissional (PRE-IA-4D): camada visual compartilhada entre Receita e
Despesas. Header com logo principal (bytes do storage local; nunca URL
externa nem ícone compacto), empresa, período pt-BR e geração
(`America/Sao_Paulo`). Filtros em labels pt-BR. KPIs só com métricas do
DTO. Footer “Dashboard Economização” + geração + `Página X de Y`.
Asset de logo ausente/corrompido não derruba o PDF. Semântica, DTO,
endpoints e XLSX permanecem inalterados.

Paridade: os bytes saem do mesmo DTO que o GET JSON e, no recorte de 1
mês, dos mesmos totais de `GET /dashboard/monthly-cash-flow` (CASH-6).
Endpoints legado `monthly-revenue` / `monthly-expenses` **não** são a
fonte oficial. Categorias permanecem distintas por identidade estável
(`kind` + `key` interno), mesmo com nomes iguais.
