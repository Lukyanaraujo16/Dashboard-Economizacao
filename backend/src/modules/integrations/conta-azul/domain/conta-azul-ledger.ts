/**
 * Tombstone R3 (CASH-8A). Default false: detecta/loga, não muta.
 * true só em testes ou CASH-8B explícito — nunca no env de produção nesta fase.
 */
export const CONTA_AZUL_LEDGER_AUTO_TOMBSTONE = false;
